node_modules
# Root-anchored so they only ignore the DAMIT viewer's build output, not
# fitsverify-web/dist (whose committed .wasm is a shipped artifact).
/dist
/dist-widget
.DS_Store
*.log
.vite
coverage
*.tsbuildinfo
playwright-report
test-results
.env
.env.local
/tmp/
/audit*.mjs
